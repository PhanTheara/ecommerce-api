networks:
  sb13-net:
    external: true
volumes:
  sb13-vol:
  keycloak-sb13-vol:
services:

  spring-sb13:
    image: spring-13:1.0.0
    build: ./spring
    container_name: spring-sb13
    ports:
      - "1333:1333"
    volumes:
      - "media.location=/Users/macbook/Documents/ecommerce-api/"
    environment:
      MEDIA_LOCATION:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-sb13:5432/ecommerce_db
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: http://localhost:1301/realms/ecommerce
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI: http://keycloak-sb13:8080/realms/ecommerce/protocol/openid-connect/certs
      # spring.datasource.url=jdbc:postgresql://localhost:1300/ecommerce_db
      # spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:1301/realms/ecommerce
      # spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:1301/realms/ecommerce/protocol/openid-connect/certs
    networks:
      - sb13-net
    depends_on:
      - postgres-sb13
      - keycloak-sb13



  keycloak-sb13:
    image: quay.io/keycloak/keycloak:26.6.4
    container_name: keycloak-sb13
    ports:
      - "1301:8080"
    command: start # start keycloak server in production
    environment:
      # Database Configuration
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres-sb13:5432/keycloak_db
      KC_DB_USERNAME: sb13usr
      KC_DB_PASSWORD: qwerqwer
      # Admin User (បណ្ដោះអាសន្ន)
      KC_BOOTSTRAP_ADMIN_USERNAME: temp_admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: temp_qwerqwer
      # Production Setting
      KC_HOSTNAME: localhost
      KC_HOSTNAME_PORT: 1301
      KC_HOSTNAME_URL: http://localhost:1301
      KC_HOSTNAME_ADMIN_URL: http://localhost:1301
      KC_HOSTNAME_STRICT: false               # set to true if HTTPS
      KC_HOSTNAME_STRICT_HTTPS: false         # set to true if HTTPS
      KC_PROXY: edge
      KC_HTTP_ENABLED: true
      #KC_HTTPS_PORT: 443
      KC_PROXY_HEADERS: xforwarded
      KC_CACHE: ispn
      KC_LOG_LEVEL: INFO
      KC_METRICS_ENABLED: true
      KC_HEALTH_ENABLED: true
      JAVA_OPTS_APPEND: "-Xms512m -Xmx2048m"
    volumes:
      - keycloak-sb13-vol:/opt/keycloak/data
      - ./keycloak/themes:/opt/keycloak/themes
      - ./keycloak/conf:/opt/keycloak/conf
    networks:
      - sb13-net
    depends_on:
      - postgres-sb13
  postgres-sb13:
    image: postgres:18.4
    container_name: postgres-sb13
    ports:
      - "1300:5432"
    environment:
      POSTGRES_PASSWORD: qwerqwer #(required)
      POSTGRES_USER: sb13usr
    volumes:
      - sb13-vol:/var/lib/postgresql/18/docker
      - ./init_db/:/docker-entrypoint-initdb.d
    networks:
      - sb13-net
